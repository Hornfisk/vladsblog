# Supabase edge function secrets — set these via `supabase secrets set`
# or in the Supabase dashboard under Project Settings > Edge Functions

# Required: Resend API key for the contact form email service
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Required: Email address that receives contact form submissions
RECIPIENT_EMAIL=your@email.com
